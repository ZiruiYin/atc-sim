[tool.pygbag]
script = "main.py"
title = "ATC-Sim"
version = "1.0"

assets = [
  "data/**",
  "core/**",
  "display/**"
]

http_headers = { Cross-Origin-Embedder-Policy = "require-corp", Cross-Origin-Opener-Policy = "same-origin" }